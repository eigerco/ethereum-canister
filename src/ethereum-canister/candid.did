service : {
    "setup": (text, text) -> ();

    "get_block_number": () -> (nat) query;

    // ERC20
    "erc20_balance_of": (text, text) -> (nat);

    // ERC721
    "erc721_owner_of": (text, text) -> (text);
}
